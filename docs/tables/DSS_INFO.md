# SQLDSS (HEC-DSS v8) #
### DSS_INFO ### 
```sql
create table dss_info(
    key text collate nocase primary key,
    value text);
  
insert into dss_info (key, value) values ('version', '8.0.0`);  
```
